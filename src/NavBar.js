const NavBar = () => {
    return (
        <nav id="navbar">
            <h3 id="logo">Cleverfool</h3>
            <div>
                <a href="/">Lobby</a>
                <a href="/createblog">Create Blog</a>
            </div>
        </nav>
    );
};

export default NavBar;
